{
  "name": "power-outage-notifier",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "main": "index.ts",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node18 --external:puppeteer --external:puppeteer-extra --external:puppeteer-extra-plugin-stealth --outfile=dist/index.js",
    "prod": "npx puppeteer browsers install chrome && npm run build && node dist/index.js",
    "dev": "ts-node index.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "node-fetch": "^3.3.2",
    "puppeteer": "^24.34.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
